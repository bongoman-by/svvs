extend type Query {
  "It's login query demo description"
  login(username: String!, password: String!): SignInResponse
  logout: Boolean
}

type SignInResponse {
  accessToken: String
  expiresIn: Int
  id: Int
}
